{
  "name": "streamhub",
  "version": "1.0.0",
  "private": true,
  "description": "StreamHub IPTV Playlist Management Platform & Web Player",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
