{
  "name": "cineverse",
  "version": "1.0.0",
  "private": true,
  "description": "Cineverse IPTV Playlist Management Platform & Web Player",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
